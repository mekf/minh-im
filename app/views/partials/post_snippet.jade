h6
  span #{post.date.getMonth()+1}.#{post.date.getDate()}.#{post.date.getFullYear()}
div.post
  h4.title
    a(href=post.url)
      = post.title
  div.post.content
    != post.preview
  .tags
    - each tag in post.tags
      a(href=tagUrl(tag))
        span.label.label-info #{tag}